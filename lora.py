import torch
import time
a_dim=49000
a = torch.zeros((a_dim,a_dim)).cuda(1)
# while True:
#     time.sleep(2)
#     a = torch.zeros((a_dim,a_dim)).cuda(1)
#     time.sleep(2)
#     a = torch.ones((a_dim,a_dim)).cuda(1)
time.sleep(360000)